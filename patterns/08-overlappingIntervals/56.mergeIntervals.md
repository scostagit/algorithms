
# ✅ **Merge Intervals — Solução Simples e Explicada Linha por Linha**

```js
/**
 * @param {number[][]} intervals
 * @return {number[][]}
 */
var merge = function(intervals) {

  // Primeiro: ordenamos os intervalos pelo início (start)
  // Por quê? Porque facilita ver quais intervalos se sobrepõem.
  intervals.sort((a, b) => a[0] - b[0]);

  // Criamos um array para guardar os intervalos já mesclados (mergeados)
  const resultado = [];

  // Percorremos um intervalo por vez
  for (let i = 0; i < intervals.length; i++) {

    // Pegamos o intervalo atual (começo e fim)
    const [inicioAtual, fimAtual] = intervals[i];

    // Se o resultado estiver vazio, NÃO TEM nada para comparar.
    // Então simplesmente colocamos o primeiro intervalo lá.
    if (resultado.length === 0) {
      resultado.push([inicioAtual, fimAtual]);
      continue; // pula para o próximo intervalo
    }

    // Pegamos o último intervalo que já está no resultado
    const ultimo = resultado[resultado.length - 1];

    // ultimo[0] = início do último intervalo já mergeado
    // ultimo[1] = fim do último intervalo já mergeado

    // Agora verificamos se existe sobreposição
    // Se o início do intervalo atual for MENOR OU IGUAL ao fim do último → eles overlap
    if (inicioAtual <= ultimo[1]) {

      // Se overlappa, atualizamos o FIM do último intervalo
      // O novo fim será o maior entre o fim antigo e o fim atual
      ultimo[1] = Math.max(ultimo[1], fimAtual);

    } else {

      // Se NÃO overlap, apenas adicionamos o intervalo ao resultado
      resultado.push([inicioAtual, fimAtual]);
    }
  }

  // No final, retornamos todos os intervalos mergeados
  return resultado;
};
```

---

# 🧠 **Explicação rápida do funcionamento**

1. **Ordenamos os intervalos** para garantir que eles estejam na ordem certa para comparação.
2. **Criamos uma lista final** (resultado).
3. Para cada intervalo:

   * Se a lista está vazia → adicionamos o primeiro.
   * Caso contrário, comparamos com o último intervalo do resultado:

     * **Se overlappa** → mesclamos (ajustamos o fim).
     * **Se não overlappa** → adicionamos como um novo intervalo separado.
4. Retornamos tudo mesclado.

---

# ✅ Exemplos funcionando

### Entrada:

```
[[1,3],[2,6],[8,10],[15,18]]
```

### Saída:

```
[[1,6],[8,10],[15,18]]
```

---


